(defvar month_offset 0)

(defpoll month
  :interval "1s"
  :initial '{"name": 0 , "num": 0}'
  "scripts/calendar.sh month"
)

(defpoll year
  :interval "1s"
  :initial '{"num": 0}'
  "scripts/calendar.sh year"
)


