(include './yuck/widgets/bar/battery.yuck')


(defwidget bar []
  (box
    :halign 'center'
    :orientation 'vertical'
    (box
      :halign 'center'
      :valign 'start'
      :orientation 'vertical'
      :space-evenly false
    )
    (box
      :halign 'center'
      :valign 'end'
      :orientation 'vertical'
      :space-evenly false
      (battery)
    )
  )
)

