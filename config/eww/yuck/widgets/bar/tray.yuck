(defwidget tray []
  (eventbox
    :class 'bar-component'
    :cursor 'pointer'
    :visible {systray_items_count > 0}
    (systray
      :class 'systray'
      :visible {systray_items_count > 0}
      :icon-size 20
      :spacing 10
    )
  )
)
