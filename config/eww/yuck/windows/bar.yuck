;; Includes main bar widget
(include './yuck/widgets/bar/main.yuck')


(defwindow bar
  :monitor 0
  :stacking 'fg'
  :exclusive true
  :namespace 'bar'
  :geometry (geometry :height '100%' :anchor 'left center')
  (bar)
)

